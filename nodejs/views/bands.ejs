<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8">
  <title>Lista zespołów</title>
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background-color: #f9f9f9;
      padding: 20px;
    }

    h1 {
      text-align: center;
      color: #333;
    }

    table {
      width: 90%;
      margin: 0 auto;
      border-collapse: collapse;
      background-color: #fff;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    th, td {
      padding: 12px 20px;
      border-bottom: 1px solid #ddd;
      text-align: left;
    }

    th {
      background-color: #4CAF50;
      color: white;
    }

    img {
      max-width: 150px;
      border-radius: 6px;
    }

    tr:hover {
      background-color: #f1f1f1;
    }

    .center {
      text-align: center;
      margin-top: 30px;
      color: #777;
    }

    .links {
      text-align: center;
      margin: 20px 0;
    }

    .links a {
      margin: 0 10px;
      text-decoration: none;
      color: #4CAF50;
    }
  </style>
</head>
<body>
  <h1>Zespoły zarejestrowane w GigMap</h1>
  <div class="links">
    <a href="/bands/new">Dodaj zespół</a> | <a href="/">Strona główna</a>
  </div>

  <% if (bands.length === 0) { %>
    <p class="center">Brak zespołów w bazie.</p>
  <% } else { %>
    <table>
      <thead>
        <tr>
          <th>Nazwa</th>
          <th>Gatunek</th>
          <th>Obrazek</th>
        </tr>
      </thead>
      <tbody>
        <% bands.forEach(band => { %>
          <tr>
            <td><%= band.name %></td>
            <td><%= band.genre %></td>
            <td>
              <% if (band.image) { %>
                <img src="/<%= band.image %>" alt="<%= band.name %>" />
              <% } else { %>
                Brak
              <% } %>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  <% } %>
</body>
</html>
